# Сделано

## 2026-08-07
- Создан `CLAUDE.md` под объём MVP (стек, архитектура, Docker-политика, языковая политика)
- Подключен Playwright MCP (project-scope, `.mcp.json`, не коммитится)
- Сохранены требования хакатона в `HACKATHON_REQUIREMENTS.md` (не коммитится)
- Скелет Docker Compose (postgres + backend + frontend), `.env.example`
- Django 5 + DRF backend: настройки под PostgreSQL, drf-spectacular, CORS
- Приложение `predictions`: модель `Prediction`, `PredictionService`, абстракция `OilSpillModel` для Hugging Face модели (`sahilvishwa2108/oil-spill-detection-models`, TensorFlow/Keras U-Net)
- API: `POST /api/predict/`, `GET /api/history/`
- Frontend: Vite + React + TS + Tailwind v4, компоненты UploadPanel/ResultCard/HistoryList
- Исправлен баг: `vite.config.ts` не содержал плагин `@tailwindcss/vite` (упавшая ранняя правка осталась незамеченной) — Tailwind не применялся вообще, страница рендерилась как plain text
- Исправлен баг: `entrypoint.sh` игнорировал переданную команду (`docker compose run ... makemigrations` тихо запускал обычный `gunicorn` вместо нужной команды) — добавлен `exec "$@"` guard
- Редизайн frontend под тёмный "спутниковый мониторинговый центр": статус-индикаторы, stat tiles, satellite feed roadmap-панель (пожелание пользователя — жюри хакатона видит дашборд первым; в будущем планируется автоматический анализ спутниковых снимков)
- E2E проверено через Playwright MCP: загрузка изображения → inference → маска сегментации → история — всё работает вживую
- **Важная находка**: изначальное предположение о порядке классов сегментации было неверным. Агент-исследователь нашёл, что тот же автор (`sahilvishwa2108`) опубликовал те же веса в документированных репозиториях `oil-spill-unet`/`oil-spill-deeplab` с явным порядком классов `[background, oil_spill, ships, look_alike, wakes]` и подтверждённым препроцессингом. Переключились на `oil-spill-unet` (файл `model.keras`, Keras 3 native format)
- **Важная находка**: модель обучена на **SAR (радарных) снимках**, не на обычных оптических фото. Тест на реальном оптическом MODIS-снимке (Deepwater Horizon, NASA, public domain) дал неверный результат ("clean"). Тест на реальном SAR-снимке (TerraSAR-X, тот же разлив, Wikimedia CC BY 3.0) дал верный результат ("oil_spill", 62.6% confidence) — подтверждает, что модель работает корректно на предназначенном для неё типе входных данных
- Исследование альтернативных моделей на HF: весь нишевый сегмент "oil spill detection" имеет 0-7 скачиваний у всех авторов, более популярной/лучше проверенной альтернативы не существует — решение остаться на текущих весах, но с документированным источником, оправдано
- Реальное тестовое SAR-изображение сохранено в `docs/sample-images/` и упомянуто в README для жюри
- README.md написан с инструкцией запуска, требованиями, описанием ограничений модели (SAR vs optical)
- QA пройден: ruff, black, mypy (backend) и `tsc --noEmit` (frontend) — все чисто
- Исправлен баг: повторная загрузка того же файла не срабатывала (`<input type="file">` не сбрасывал value) — теперь сбрасывается после каждого выбора
- Исправлен баг: клик по зоне загрузки не открывал системный диалог выбора файла в реальном браузере (`div onClick + ref.click()` ненадёжен) — переписано на нативный `<label htmlFor>` + `<input>`, что гарантированно работает во всех браузерах
- Добавлен сброс формы загрузки после успешного предсказания (`resetKey` проп)
- History: добавлен скролл (`max-h-[520px] overflow-y-auto`) и клик по элементу открывает его как полную карточку результата (площадь разлива, confidence, маска) — раньше показывался только последний результат
- Добавлена легенда цветов маски сегментации (`MaskLegend`) — раньше было непонятно, какой цвет соответствует какому классу (background/oil_spill/ships/look_alike/wakes)
- Добавлена поддержка 3 языков интерфейса (EN/RU/KK) через лёгкий кастомный i18n-контекст (`src/i18n/`), переключатель в шапке, сохранение выбора в localStorage
- Добавлена легенда цветов маски и проверена ориентация маски (совпадает с оригиналом, не перевёрнута) — по просьбе пользователя после сомнений в результате
- **Важная находка**: два предыдущих тестовых изображения (TerraSAR-X Deepwater Horizon, Balikpapan-инфографика) давали физически неправдоподобный результат (50%+ площади = "ships"). Расследование показало: модель хорошо работает только на **сырых, неаннотированных** SAR-сценах — на готовых инфографиках/картах с текстом и псевдоцветом (как Balikpapan) она ломается. Проверено на чистом Sentinel-1 снимке от ESA (28 июня 2017): результат стал правдоподобным (97.5% фон, 0.8% нефть на первом прогоне без API; 60% conf / 0.8% площади через реальный API). Тестовое изображение в репозитории заменено на этот чистый снимок, README обновлён с честным объяснением ограничения
- Проверены ещё 2 доп. ESA-снимка (Mediterranean slick, Oil spill spread, оба Sentinel-1, окт. 2018) — оба оказались "пресс-релизными" композитами (чёрная рамка, врезанный zoom-инсет, текстовые метки с датой/масштабом), не сырыми сценами — модель дала мусорный результат на обоих (18-25% ложных "ships"/"wakes"). В seed НЕ добавлены — только честно проверенный ESA 2017 снимок остаётся эталонным тестовым примером
- Добавлена Django management-команда `seed_demo_data`: при первом запуске (пустая БД) автоматически прогоняет эталонный ESA Sentinel-1 снимок через `PredictionService`, чтобы дашборд не был пустым у жюри/новых разработчиков. No-op если предсказания уже есть. Вызывается из `entrypoint.sh` при старте контейнера. Проверено на полностью чистом старте (`docker compose down -v && up --build`) — работает
- README дополнен разделом "Why SAR, not ordinary satellite photos?" — объясняет разницу SAR/оптика (как детектируется нефть, погода/время суток, стоимость данных, удобство), и указывает, что точный тренировочный датасет модели не задокументирован автором (предположение по классам, не факт)
- Найден и исправлен ещё один потенциальный источник бага "клик по upload-зоне не открывает диалог": родительский `<label>` не имел `position: relative`, из-за чего абсолютно спозиционированный скрытый `<input>` мог визуально уезжать за пределы label в некоторых браузерах/раскладках — добавлен `relative` на label
